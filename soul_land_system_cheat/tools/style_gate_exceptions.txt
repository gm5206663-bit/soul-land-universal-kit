# Recorded exceptions to the the-way ban (substring per line, with reason).
# Every line must cite the footer that records the keep.
quietly, the way an orchard begins, one root at a time   # Ch1 closing image - kept on purpose (Ch1 footer: "the-way tic x1, the closing image, kept on purpose")
